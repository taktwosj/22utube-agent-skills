# Stage 04 External Review Contract

Load only for resolved Stage 04. For `URAKKAI`, complete exactly two review-improvement loops: first-party Claude OAuth with Claude Opus at `--effort low`, then Hermes improvement; repeat once on the improved candidate. Hermes selects the best candidate while retaining authority over segment IDs, approved source/target ranges, and actual audio timing. If a Claude loop fails, use one Hermes subagent review for that failed loop only. Record input/output hashes, improvements, accepts/rejections, and the final reason in both `20_script/external-review.md` and `20_script/external-review.json`; never record tokens, cookies, OAuth values, URLs, or session IDs. Missing loop evidence is `WAIT_EXTERNAL_REVIEW`. Do not force this loop for `SOURCE_ORDER_UNCHANGED_CLEAN_ONLY`.
