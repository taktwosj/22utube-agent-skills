interface:
  display_name: "119 정치롱폼 CapCut 카드 조립"
  short_description: "잠긴 정치롱폼 입력을 휴대형 근본으로 CapCut 프로젝트에 조립"
  default_prompt: "Use $119-politics-longform-capcut. Detect and validate a direct PRE-119 handoff before direct-script fallback; otherwise use the matching direct or legacy route. Compile episode_cards.json only after real A/D and requested B/C evidence, then build a clean local CapCut draft and report relink and visual gates separately."
