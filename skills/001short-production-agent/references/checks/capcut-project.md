# CapCut 실제 프로젝트 검증

working copy를 validator가 `draft_content`, meta, Timelines, Resources에서 직접 검사해 증거로 만든다. schema·ID mirror·material·geometry·시간순서·media decode가 모두 맞아야 `CAPCUT_STATIC_VALIDATED`다. 선행 입력이 없으면 WAIT이며 외부 호출은 금지다.
