# 사람눈 설계도 — <회차 제목>

> 이 파일은 JSON이나 production plan을 보여주기 전, 운영자가 내용을 눈으로 승인하기 위한 문서다.

## 1. 영상 한 줄 요약

<누가, 어떤 상황에서, 무엇을 하는 영상인지 1~2문장>

## 1-1. 원본 쇼츠 분류

| 축 | 확정값 | 근거 |
|---|---|---|
| 소재 `subject_category` |  |  |
| 화면 템플릿 `presentation_template` |  | `baked_order_semantics`:  |
| 전달 방식 `delivery_mode` |  |  |
| 원본 서사 `source_structure_pattern` |  |  |
| 최종 실행 `execution_strategy` |  | 원본 전달 방식과 별도 판단 |

> 허용값과 선택 기준은 `references/shorts-structure-taxonomy.md`를 따른다. 이 표는 추측이 아니라 원본 관찰 근거를 쓴다.

## 2. 우라까이 구조 변경

### 2-0. 상황별 원본 구조 구분 — 순서 설계보다 먼저

구조 개수와 번호를 미리 정하지 않는다. 원본을 처음부터 끝까지 본 뒤 다음 변화점에서 구조를 나눈다.

- 상황·장소 변화
- 행동 목적 변화
- 주도 화자·대화 관계 변화
- 전달 방식 변화: 화자 발언/TTS/나레이션/효과음/영상 재생
- 서사 기능 변화: 도입/충돌/검증/행동/반전/결과/결말

| 원본 구조 | source range | 상황·행동 | 주도 화자 | 전달 방식 | 서사 기능 | 구조 분리 근거 |
|---:|---:|---|---|---|---|---|
| 구조 1 |  |  |  |  |  | 직전·다음 구간과 무엇이 달라지는지 기록 |

> 특정 구조의 A/B 분할은 필수가 아니다. 같은 원본 상황 안에서도 앞·뒤의 기능이 실제로 달라 별도 이동이 필요할 때만 사용한다.

- 원본 구조: `<상황별 분석 후 확정한 순서>`
- 최종 구조: `<예: 3→4→1→2→5>`
- 원본 order signature: `<B1>B2>B3...>`
- 최종 order signature: `<B3a>B1>B2...>`
- 우라까이 구조: `<DST_...>`
- 판정: `<TRANSFORM_CANDIDATE/TRANSFORM_APPROVED/SAFE_UNCHANGED_FALLBACK/BLOCKED_REFERENCE>`

### 2-1. 변경된 수정 구조 — 먼저 표시

| 수정 후 시간 | 최종 순번 | 가져온 원본 구조 | source range | target range | 전달 방식 | 구조 유형 | 상황 설명·재배치 이유 |
|---:|---:|---:|---:|---:|---|---|---|
|  | 1 |  |  |  | 화자 발언/단순 TTS/나레이션/효과음만/영상 재생만/복합형 | 훅/충돌/맥락/검증/결말 |  |

### 2-2. 원본 설계도 — 뒤에 표시

| 원본 시간 | 원본 구조 | 전달 방식 | 구조 유형 | 상황 설명 |
|---:|---:|---|---|---|
|  | 구조 1 | 화자 발언/단순 TTS/나레이션/효과음만/영상 재생만/복합형 | 도입/문제 제기/변명/대립/비교/검증/행동/결과/결말 |  |

> 전달 방식은 실제 A9/A10·현장음 구성을 기준으로 쓴다. A10 원본 음성이 살아 있으면 `영상 재생만`으로 표시하지 않는다.

> 원본 구조와 최종 구조가 같으면 `URAKKAI_STRUCTURE_UNCHANGED`로 중단한다. 제목·크롭·TTS 변경만으로 우라까이 승인을 내리지 않는다.

## 3. 핵심 메시지

> <시청자가 마지막에 이해해야 할 한 문장>

## 3-1. 가단야·와우포인트

| 항목 | 확정 내용 |
|---|---|
| 가: 가이드라인 | 시청자에게 남길 메시지, 타깃 감정, 금지선, 미해결 질문 |
| 단: 단어변경 | 새 정보 순서에 맞춘 제목·상황 자막·핵심 단어 |
| 야: 야부리 (와우포인트로 순서 바꾸기) | 와우포인트를 0~3초로 올린 새 컷 순서 + 그 재배열에 필요한 훅/브리지/대사 진입/회수 TTS |
| 와우포인트 | 원본 시간, 무엇이 보이는지, 왜 첫 장면인지, 함께 옮길 의존 묶음 |

> 와우포인트를 복제하고 원본 본문을 그대로 재생하지 않는다. 원본 발언, 상황 자막, TTS 해석은 각각의 역할을 분리한다.

## 4. 제목 설계

- T1: `<확정 문구>`
- T2: `<확정 문구>`
- 노출 시간: `<start>~<end>`
- 보존: 근본 프로젝트의 폰트·크기·위치·색상·외곽선·배경·정렬·애니메이션

## 4. 오프닝 내레이션

- A9 문구: `<확정 문구 또는 미사용>`
- 시간: `<start>~<end>`
- 원본 음성 처리: `<유지/음소거/duck>`

## 5. 시간별 편집 설계

| 시간 | 화면/사건 | 들리는 소리 | 한국어 핵심 자막 STATE | 편집 의도 |
|---|---|---|---|---|
| 0.0~0.0초 |  |  |  |  |

## 6. 마지막 한 줄

`<결론 또는 미사용>`

## 7. 화면 구성

- 비율·해상도·FPS
- 인물/중요 사물 가림 금지 영역
- crop/zoom/전환 원칙
- 기존 baked-in 제목·자막 존재 여부
- clean 처리 방식 또는 중복 자막 방지 방식

## 8. 자막 방식

- STATE 밀도와 줄 수
- A10_TEXT 전체 번역 사용 여부
- 기존 baked-in 자막과의 충돌 처리

## 9. 오디오 설계

- A9: `<TTS/미사용>`
- A10: `<원본 음성 구간>`
- A11: `<효과음/미사용>`
- A12: `비움`

## 10. 근본 anchor 배치

| Anchor | 들어갈 내용 |
|---|---|
| VIDEO |  |
| T1 |  |
| T2 |  |
| A9 |  |
| A10 |  |
| STATE |  |
| A11 |  |
| A12 |  |

## 11. 완성 후 시청자가 이해할 내용

1.
2.
3.

## 12. 운영자 승인 선택지

- 추천안 그대로 진행
- T1/T2 수정
- 오프닝 TTS 없이 원본 음성만 사용
- clean 방식 수정

추천안의 실제 문구와 표를 먼저 보여준 뒤에만 위 선택지를 묻는다.
