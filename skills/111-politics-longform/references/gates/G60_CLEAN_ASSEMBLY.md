# G60 — Clean Politics Assembly

Main longform clones only `jungchilong_base_v3_intro15`; derived Shorts clone
only `SHRTJUNGCHI`. Never use a failed prior project as input.
`REFERENCE_ONLY` in materials or timeline, unapproved visible text,
source/order/hash mismatch, or structural contamination is a hard fail.
Structural contamination requires a clean rebuild. Static PASS transitions to
`WAIT_USER_VISUAL_GATE` and never implies user visual PASS.

