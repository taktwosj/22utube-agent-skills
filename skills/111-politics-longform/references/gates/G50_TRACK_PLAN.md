# G50 — Politics Track Plan

Consume the exact G40 caption lock and preserve timeline order. Apply only roles
declared by the lane schema. Narration audio is allowed only in `narrated`
mode. The plan locks asset paths, SHAs, visible text roles, audio policy, and
the selected root for the profile.

