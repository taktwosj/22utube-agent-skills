# G00 — Politics Intake Lock

Lock `episode_id`, source fingerprint, requested target, `content_profile`, and
`production_mode`. Profiles are `politics_longform` and
`politics_derived_short`; modes are `source_led` and `narrated`.

Changing either lock after G00 requires `WAIT_USER_EDITORIAL_CONFIRMATION`.
Derived Shorts remain owned by `111-politics-longform` and use `SHRTJUNGCHI`.

