# G70 — Upload Package

Requires a user-authored `USER_VISUAL_PASS` event on `G60.USER`. Package title,
description, thumbnail, and upload metadata with `release_allowed=false`.
Preparing the package does not upload; actual upload remains
`WAIT_UPLOAD_APPROVAL`.

