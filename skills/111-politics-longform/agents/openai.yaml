interface:
  display_name: "111 Politics Longform"
  short_description: "Politics longform and derived Shorts"
  default_prompt: "Use $111-politics-longform for politics-longform or its derived Shorts across G00 through G90. Read workflow.yaml, resolve the active gate, and load exactly that gate reference. Keep derived Shorts in this lane using SHRTJUNGCHI; do not cross-load another lane."
